"# UML Use Case Diagram â€” Visual Inspection Robot\n\nThis document contains the UML Use Case Diagram representing user interactions with the Flask Web Console for the Visual Inspection Robot (Zadanie 19).\n\n```mermaid\nusecaseDiagram\n    actor Operator as \"Robot Operator\"\n\n    %% User Account Management Use Cases\n    Operator --> (Register Account)\n    Operator --> (Log In)\n    Operator --> (Log Out)\n\n    %% Dashboard and System telemetry Use Cases\n    Operator --> (View Dashboard)\n    Operator --> (View Event Logs & History)\n\n    %% Inspection Setup Use Cases\n    Operator --> (Select Inspection Route)\n\n    %% Action Control Use Cases\n    Operator --> (Start Inspection Run)\n    Operator --> (Halt Inspection Run)\n    Operator --> (Emergency Stop)\n    \n    %% Relationships\n    (Select Inspection Route) ..> (View Dashboard) : <<include>>\n    (Start Inspection Run) ..> (Select Inspection Route) : <<precedes>>\n    (Halt Inspection Run) ..> (Start Inspection Run) : <<extends>>\n    (Emergency Stop) ..> (Start Inspection Run) : <<extends>>\n```\n\n## Detailed Actor and Use Cases Description\n\n### 1. Primary Actor\n* **Robot Operator**: An authenticated user authorized to configure routes, trigger robot navigation, view live feedback logs, and enforce safety overrides (E-Stop).\n\n### 2. Core Use Cases Description\n* **Register Account**: Creates a new user record in the SQLite database with hashed password credentials.\n* **Log In / Log Out**: Manages active sessions and restricts robot controls to authenticated operators.\n* **View Dashboard**: Renders real-time telemetry variables (Robot State, Visited Checkpoints, Current Command, Active Run ID).\n* **Select Inspection Route**: Configures the inspection path (Route Alpha, Short Loop) in the system parameters.\n* **Start / Halt Run**: Initialized autonomous path navigation or stops the current route cleanly.\n* **Emergency Stop (E-Stop)**: Enforces an immediate cut-off of motor inputs and puts the system into an ERROR state.\n* **View Even
<truncated 111 bytes>