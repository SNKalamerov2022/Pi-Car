"# Visual Inspection Robot - Flask Web Console\n\nAn organized, modular Flask application that serves as the operator control terminal for the **Visual Inspection Robot** (Zadanie 19). It includes operator authentication, database tables, a cyber-themed status dashboard, and placeholder control hooks.\n\n---\n\n## 1. Project Purpose\nThe console provides high-level control of the autonomous inspection robot:\n* **Authentication**: Limits system controls to registered robot operators.\n* **Route Configuration**: Selects different path templates (Route Alpha, Short Loop).\n* **Telemetry Monitoring**: Displays status indicators (Following Route, Stopped for Inspection, Completed, E-Stop).\n* **System Logging**: Stores timestamps, route run history, and verification events.\n\n---\n\n## 2. Installation & Setup\n\n### Step 1: Install Dependencies\nEnsure you have Python 3 installed. Navigate to this directory and install required packages:\n```bash\npip install -r requirements.txt\n```\n\n### Step 2: Database Initialization\nThe SQLite database (`instance/robot_inspection.db`) is initialized and seeded **automatically** on the first execution. No manual script runs are needed.\n\n### Step 3: Run the Application\nStart the Flask development server:\n```bash\npython run.py\n```\nThe server will start running on `http://localhost:5000/`.\n\n---\n\n## 3. Usage Instructions\n1. Open `http://localhost:5000/` in your web browser.\n2. Go to **Register** and create a new operator account.\n3. Log in with your new credentials to access the secure console.\n4. Go to **Select Route** in the sidebar, select a route loop (e.g. *Route Alpha*), and click **Configure**.\n5. On the **Dashboard**, click **START INSPECTION** to simulate autonomous route execution, checkpoint stops, target photo triggers, and log tracking.\n6. Press the **EMERGENCY STOP (E-STOP)** button to trigger a critical safety shutdown.\n7. Go to **Logs & History** to review logged entries and previous execution states.\n\n---\n\n## 4. Current Limitations & Future W
<truncated 531 bytes>