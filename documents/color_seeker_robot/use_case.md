"# UML Use Case Diagram â€” Color Target Seeker Robot\n\nThis document contains the UML Use Case Diagram representing user interactions with the Flask Web Console for the Color Target Seeker Robot (Zadanie 3).\n\n```mermaid\nusecaseDiagram\n    actor Operator as \"Robot Operator\"\n\n    %% User Account Management Use Cases\n    Operator --> (Register Account)\n    Operator --> (Log In)\n    Operator --> (Log Out)\n\n    %% Dashboard and System telemetry Use Cases\n    Operator --> (View Dashboard)\n    Operator --> (View Event Logs & History)\n\n    %% Seeker Configuration Use Cases\n    Operator --> (Configure Target Color)\n\n    %% Action Control Use Cases\n    Operator --> (Start Color Scanning)\n    Operator --> (Halt Search)\n    Operator --> (Emergency Stop)\n    \n    %% Relationships\n    (Configure Target Color) ..> (View Dashboard) : <<include>>\n    (Start Color Scanning) ..> (Configure Target Color) : <<precedes>>\n    (Halt Search) ..> (Start Color Scanning) : <<extends>>\n    (Emergency Stop) ..> (Start Color Scanning) : <<extends>>\n```\n\n## Detailed Actor and Use Cases Description\n\n### 1. Primary Actor\n* **Robot Operator**: An authenticated user authorized to configure search bounds, trigger scanning operations, monitor target detection states, view live telemetry logs, and enforce safety overrides (E-Stop).\n\n### 2. Core Use Cases Description\n* **Register Account**: Creates a new user record in the SQLite database with hashed password credentials.\n* **Log In / Log Out**: Manages active sessions and restricts robot control and telemetry access to authenticated operators.\n* **View Dashboard**: Renders real-time telemetry variables (Robot State, Target Spotted Flag, Proximity Distance, Active Run ID) and visual camera tracking simulator.\n* **Configure Target Color**: Selects the active target search color (Red, Green, Blue, Yellow) to calibrate the OpenCV HSV threshold filters.\n* **Start Color Scanning**: Initiates autonomous rotational scan and approaches the target color once identifie
<truncated 376 bytes>