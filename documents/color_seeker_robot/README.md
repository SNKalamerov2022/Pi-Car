"# Color Target Seeker Robot - Flask Web Console\n\nAn organized, modular Flask application that serves as the operator control terminal for the **Color Target Seeker Robot** (Zadanie 3). It includes operator authentication, database tables, a cyber-themed target scanner status dashboard, and placeholder control hooks.\n\n---\n\n## 1. Project Purpose\nThe console provides high-level control of the autonomous target-seeking robot:\n* **Authentication**: Limits system controls to registered robot operators.\n* **Target Configuration**: Selects the color range coordinates (Red, Green, Blue, Yellow) for OpenCV filtration.\n* **Telemetry Monitoring**: Displays status indicators (Scanning, Target Spotted, Approaching, Completed, E-Stop) and estimated distance tracking in centimeters.\n* **System Logging**: Stores timestamps, search run history, and detection logs.\n\n---\n\n## 2. Installation & Setup\n\n### Step 1: Install Dependencies\nEnsure you have Python 3 installed. Navigate to this directory and install required packages:\n```bash\npip install -r requirements.txt\n```\n\n### Step 2: Database Initialization\nThe SQLite database (`instance/robot_seeker.db`) is initialized and seeded **automatically** on the first execution. No manual script runs are needed.\n\n### Step 3: Run the Application\nStart the Flask development server:\n```bash\npython run.py\n```\nThe server will start running on `http://localhost:5001/` (port 5001 is used to prevent port conflicts if running alongside other robot systems).\n\n---\n\n## 3. Usage Instructions\n1. Open `http://localhost:5001/` in your web browser.\n2. Go to **Register** and create a new operator account.\n3. Log in with your new credentials to access the secure console.\n4. Go to **Configure Target** in the sidebar, select a color target filter (e.g. *Red Target Search*), and click **Configure**.\n5. On the **Dashboard**, click **START SCANNING** to simulate rotational search, hue threshold matching, camera alignment adjustment, and approach telemetry tracking.\n6. Press t
<truncated 690 bytes>